# Environment management
python-dotenv==1.0.0

# Asynchronous HTTP client
aiohttp==3.8.5

# Data validation and settings management using Python type annotations
pydantic==1.10.13

# Core library for working with LangChain, a framework for LLM operations
langchain==0.0.232

# Integration with OpenAI models through LangChain
langchain-openai==0.0.6

# NVIDIA AI Endpoints for specialized embeddings and reranking models
langchain-nvidia-ai-endpoints==0.1.0

# ASGI server for serving FastAPI applications
uvicorn==0.23.2

# FastAPI for building APIs, if used in your project
fastapi==0.100.0

# Type extensions for newer Python typing capabilities
typing-extensions==4.8.1

# Additional utilities for structured outputs and parsing
langchain-core==0.0.8  # Assumed core component for parsing and structured output

# JSON and YAML parsing libraries for configurations
pyyaml==6.0

# Utilities for handling JSON in a more flexible way
orjson==3.9.6

# For handling specific JSON schema requirements, if needed
jsonschema==4.19.0

# Docker SDK for Python, if interacting with Docker programmatically
docker==6.1.3

# HTTPX for advanced HTTP requests (optional, replace aiohttp if preferred)
httpx==0.25.1

# Additional tools for managing long-running tasks and asynchronous processes
asyncio==3.4.3

# Logging configuration for better logging and debugging support
loguru==0.7.0

# Testing libraries for unit and integration testing
pytest==7.5.2
pytest-asyncio==0.21.1

# Development tools
black==23.9.1  # Code formatting
flake8==6.1.0  # Linting
isort==5.12.0  # Import sorting

# Security and audit tools
bandit==1.7.5  # Security linter
safety==2.5.1  # Dependency vulnerability checks

# Caching and persistence (if used for faster LLM responses)
diskcache==5.6.1

# Specific handling for AI model wrappers or custom integrations
huggingface-hub==0.18.0  # For Hugging Face model management
transformers==4.34.0  # For custom LLMs if used outside OpenAI/NVIDIA API

# Dependencies specific to the NVIDIA stack if any additional packages are needed
nvidia-pyindex  # NVIDIA specific index for their custom packages